"""
JobNameGoesHere

About this job...
"""

from transporter import log
from transporter.utils import Timer


# Configure your data sources here
# source = SomeDB()
# target = MyDB()


def run():
    # Simple job example:
    # log.info('Running job JobNameGoesHere')
    # log.info('Selecting a user data from table users')
    #
    # timer = Timer()
    # cargo = source.extract('SELECT * FROM users WHERE MONTH(date_from) = MONTH(NOW())')
    # target.load(cargo)
    #
    # log.info('Loaded %s rows in %s' % (cargo.size, timer)
    return